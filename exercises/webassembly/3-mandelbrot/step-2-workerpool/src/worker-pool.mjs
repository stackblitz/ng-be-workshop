import * as Comlink from 'https://unpkg.com/comlink/dist/esm/comlink.mjs';

const workers = [];

export function spawnWorkers(count) {
  // return the requested amount of workers, create new workers if needed
}
